version: '3'

services:
  db:
    image: postgres:15.2
    container_name: foodPlannerDB
    environment:
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    ports:
      - "5555:5432"
    volumes:
      - ./postgres:/var/lib/postgresql/data
    restart: always
    